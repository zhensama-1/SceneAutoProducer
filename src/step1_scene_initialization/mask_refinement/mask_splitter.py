from __future__ import annotations

from src.step1_scene_initialization.schemas import DetectedObject


class MaskSplitter:
    """Placeholder boundary for future mixed-object mask splitting."""

    def split_mixed_masks(self, detections: list[DetectedObject]) -> list[DetectedObject]:
        return detections
